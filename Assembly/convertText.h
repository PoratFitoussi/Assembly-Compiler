#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

#ifndef mask
#define mask 1
#endif

/* A function that get two strings and return the combination of the two of them*/
char *concat(const char *s1, const char *s2);
/* A function that convert string to number */
int textToNum(char *text, int from, int end);

